spring:
  data:
    cassandra:
      schema-action: create_if_not_exists
      request:
        timeout: 10s
      connection:
        connect-timeout: 50s
        init-query-timeout: 50s


  config:
    import: secure.yml

datastax.astra:
  secure-connect-bundle: secure-connect-betterreads.zip

# spring.main.allow-circular-references: true


